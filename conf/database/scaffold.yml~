BASE:

  PAGE:
    display:		[ permlink, controller, view ]
    create:		{ name: text, controller: phpeditor, view: fckeditor }

  TEMPLATE:
    display:		[ permlink, controller, structure, templateview, view ]
    create:		{ name: text, controller: phpeditor, structure: textarea, templateview: fckeditor, view: fckeditor }

  FORM:
    display:		[ permlink, preprocess, structure, process, view ]
    create:		{ name: text, preprocess: phpeditor, structure: textarea, process: phpeditor, view: fckeditor }

#---------------------------------------------
USER_CENTRAL:

  USER:
    display:		[ permlink, username, password, email ]
    create:		{ name: text, username: text, password: password, email: text }

  PERMISSION:
    display:		[ permlink, name ]
    create:		{ name: text }

  PGROUP:
    display:		[ permlink, name ]
    create:		{ name: text }

#---------------------------------------------
MALLOCWORKS:

  SUBSCRIBER:
    display:		[ permlink, email ]
    create:		{ name: text, email: text }


  CATEGORY:
    display:		[ permlink, name ]
    create:		{ name: text }
  
  WALL:
    display:		[ permlink, name ]
    create:		{ name: text }

  RESOURCE:
    display:		[ permlink, name ]
    create:		{ name: text }

  EMAIL:
    display:		[ permlink, name ]
    create:		{ name: text }

  ANNOUNCEMENT:
    display:		[ permlink, name ]
    create:		{ name: text }
